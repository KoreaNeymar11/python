# 변수:값에 붙이는 이름
# pi=3.14

# 변수를 만드는 방법
# 식별자=값
pi=3.141592

# 변수를 사용하는 방법
print(pi)

# 변수와 관련된 용어
# 정의(define):변수를 만드는 행위+선언

# 할당(assign):변수에 값을 넣는 행위
# 창조(reference):변수 안에 있는 값을 사용하는 행위

# 정의와 할당
pi=3.141592
pi=3.14
r=10

print("원주율",pi)
print("반지름",r)
print("원의 둘레",2*pi*r)
print("원의 넓이",pi*r*r)

# 복합 대입 연산자
a+=1 #a=a+1
a-=1 #a=a-1
string="안녕하세요"
string+="!"

